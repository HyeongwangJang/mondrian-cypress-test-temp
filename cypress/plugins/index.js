/// <reference types="cypress" />

/**
 * @type {Cypress.PluginConfig}
 */
module.exports = (on, config) => {
  // require('../../nodemailer')(on, config, {
  //   email: ['hgjang@sphinfo.co.kr'],
  //   emailOnSuccess: true,
  //   dry: false,
  // })
}